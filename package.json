{
  "name": "content-flow-e2e",
  "private": true,
  "description": "Dev-only Playwright harness for the wizard flows. Does not affect the extension's own runtime JavaScript, which stays plain ES modules loaded via TYPO3's importmap - no build step, no bundler.",
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
