{
  "name": "content-flow-e2e",
  "private": true,
  "type": "module",
  "description": "Dev-only JavaScript test tooling: vitest for the extension's own ES modules, Playwright for the backend journeys. Neither builds or bundles the runtime JavaScript, which stays plain ES modules loaded via TYPO3's importmap.",
  "scripts": {
    "test:js": "vitest run",
    "test:js:watch": "vitest",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "jsdom": "^29.1.1",
    "vitest": "^4.1.10"
  }
}
